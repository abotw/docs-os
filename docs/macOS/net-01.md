在 **macOS** 上，可以通过以下常用**命令行工具**来查看网络状态与诊断问题。

------

## 一、查看网络接口与 IP 信息（最常用）

### 1️⃣ 查看所有网络接口

```bash
ifconfig
```

-   `en0`：通常是 **Wi-Fi**
-   `en1 / en2`：有时是有线或虚拟网卡
-   `lo0`：本地回环（127.0.0.1）

只看 Wi-Fi：

```bash
ifconfig en0
```

------

### 2️⃣ 查看当前 IP 地址（推荐）

```bash
ipconfig getifaddr en0
```

查看所有接口 IP：

```bash
ipconfig getifaddr en0
ipconfig getifaddr en1
```

------

## 二、查看网络连接状态

### 3️⃣ 查看路由表（是否有默认网关）

```bash
netstat -rn
```

关键一行：

```text
default            192.168.1.1
```

没有 `default` → 网络通常无法访问外网。

------

### 4️⃣ 查看当前网络配置（官方工具）

```bash
scutil --nwi
```

可看到：

-   当前使用的接口
-   DNS / IPv4 / IPv6 状态
-   是否联网（Reachable）

------

## 三、Wi-Fi 专用查看（macOS 特有）

### 5️⃣ 查看 Wi-Fi 连接信息（信号、速率）

```bash
/System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport -I
```

重点字段：

-   `SSID`：当前 Wi-Fi 名称
-   `agrCtlRSSI`：信号强度（-30 很好，-80 很差）
-   `lastTxRate`：当前速率

------

### 6️⃣ 扫描附近 Wi-Fi

```bash
/System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport -s
```

------

## 四、连通性测试（非常关键）

### 7️⃣ 测试局域网

```bash
ping 192.168.1.1
```

### 8️⃣ 测试公网

```bash
ping 8.8.8.8
```

### 9️⃣ 测试 DNS 是否正常

```bash
ping www.baidu.com
```

👉 能 ping IP，不能 ping 域名 = **DNS 问题**

------

## 五、查看 DNS 配置

### 🔟 查看当前 DNS

```bash
scutil --dns
```

或简洁版：

```bash
networksetup -getdnsservers Wi-Fi
```

------

## 六、查看当前连接和端口

### 1️⃣1️⃣ 查看当前 TCP/UDP 连接

```bash
netstat -an
```

### 1️⃣2️⃣ 查看监听端口（配合进程）

```bash
lsof -i -n -P
```

例如：

```bash
lsof -i :22
```

------

## 七、网络诊断一条龙（强烈推荐）

### 1️⃣3️⃣ macOS 自带诊断

```bash
networkQuality
```

（macOS Monterey+）

显示：

-   上行 / 下行带宽
-   延迟
-   响应质量（很好用）

------

## 八、常见排错组合（实战）

```bash
ifconfig en0
ipconfig getifaddr en0
netstat -rn
ping 8.8.8.8
scutil --dns
```

